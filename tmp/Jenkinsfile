@Library('freelance') _
import com.example.*

pipeline {
   agent any
   stages {
        stage('library groovy') {
			steps {
				script {
					def hamada = new Pipeline(this, "config.yaml")
					println(hamada.configurationFile)
					hamada.execute
				}
			}
		}				
	}
}
